# Controllers

ASP.NET Core Web API controllers are the transport boundary. Controllers should remain thin: bind and authorize HTTP requests, dispatch Application commands or queries, and map results to HTTP responses.

# Features

Business capabilities are vertical slices below this directory. Each feature owns its commands, queries, DTOs, validators, mappings, and contracts. This foundation intentionally contains no product feature implementation.

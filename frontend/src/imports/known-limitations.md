# Viewer known limitations

- Production map tiles and geographic clustering are intentionally deferred behind the typed map boundary.
- Search results are local fixture results and do not perform network requests from page components.
- Preferences are demonstrated locally and are not synchronized to an account.
- Report preview content is read-only and uses deterministic summary data.

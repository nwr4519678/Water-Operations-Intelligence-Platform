import { LockKeyhole, Mail, ShieldCheck, UserRound } from 'lucide-react'
import { PageHeading } from '../components/ui/PageHeading'
import { useAuth } from '../hooks/useAuth'
export function Profile() { const { session } = useAuth(); return <><PageHeading eyebrow="ACCOUNT / PROFILE" title="Viewer profile" description="Your account has view-only permissions across the EchoCloud platform." /><section className="profile-grid"><article className="profile-card panel"><span className="profile-avatar"><UserRound size={30} /></span><h2>MERI Viewer</h2><p>Water & Marine Environmental Research Institute</p><span className="viewer-role"><ShieldCheck size={16} /> Viewer role</span></article><article className="account-details panel"><h2>Account details</h2><div><span><Mail size={18} /> Email</span><strong>{session?.email}</strong></div><div><span><LockKeyhole size={18} /> Access scope</span><strong>Read-only telemetry and reports</strong></div><div><span><ShieldCheck size={18} /> Session security</span><strong>Mock JWT session protected</strong></div></article></section></> }

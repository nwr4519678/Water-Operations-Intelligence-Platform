# Services

Services expose typed ports and adapters. Mock adapters are deterministic and
are used by the Viewer foundation. Production HTTP and realtime implementations
must be added behind the same ports and never called directly by pages.

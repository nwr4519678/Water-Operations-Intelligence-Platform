{
  "mcpServers": {
    "clickup_community": {
      "command": "python",
      "args": ["${PLUGIN_DIR}/clickup_community_server.py"],
      "env": {
        "CLICKUP_API_TOKEN": "${CLICKUP_API_TOKEN}",
        "CLICKUP_WORKSPACE_ID": "${CLICKUP_WORKSPACE_ID}"
      }
    }
  }
}

